
In this branch (`react_and_typescript_rewrite`), I rewrite the two minesweeper pages in react and typescript (the `select_difficulty` page, and the `main` page).
After that, you can merge this branch with the main branch. Don't delete the existing javascript files that contain the game logic.
Then create another branch and rewrite the game logic in react and typescript.
