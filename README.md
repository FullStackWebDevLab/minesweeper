# Minesweeper

This repository contains two different versions of minesweeper;
    the first version runs fully on the browser without a backend,
    the second version has a backend.

Both are the same standard minesweeper game.

The frontend only version is in the [static](./static) directory, while the fullstack version is in the [fullstack](./fullstack) directory.

---
